interface:
  display_name: "Ego Browser Operator"
  short_description: "Use ego-browser with explicit authenticated-task controls"
  default_prompt: "Use $ego-browser-operator to operate or troubleshoot ego-browser within the requested website task."
policy:
  allow_implicit_invocation: true
